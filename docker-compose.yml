services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: reachout
      POSTGRES_USER: reachout
      POSTGRES_PASSWORD: reachout_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
